# Лабораторная работа №3 (ещё не сдаём)

Этот каталог — только задел. Практическая работа №2 сдаёт само веб-приложение.
В третьей работе поверх него поднимается Nginx на отдельной VM:

- 192.168.xx.11 — балансировщик
- 192.168.xx.12 и 192.168.xx.13 — два экземпляра этого приложения

Приложение уже к этому готово:

- слушает только HTTP, TLS не обрабатывает
- отдаёт `X-Backend-Instance` и страницу `/status`
- health-check: `GET /api/health`, readiness: `GET /api/ready`
- сессии в Redis, данные в PostgreSQL — переживают падение одной ноды
- асинхронные эндпоинты: `/api/v1/coverage/scan`, `/api/v1/events`

Playbook Ansible, DNS-балансировку и SSL-терминацию добавляем в работе №3.
